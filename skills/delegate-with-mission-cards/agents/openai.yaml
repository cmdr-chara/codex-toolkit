interface:
  display_name: "Mission Control"
  short_description: "Delegate bounded work with mission cards and verify every result"
  default_prompt: "Use $delegate-with-mission-cards to decompose suitable work into bounded missions, select the lightest capable roles, run safe parallel waves, and verify every result."
