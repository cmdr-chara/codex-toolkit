interface:
  display_name: "Multi-Agent Work Coordinator"
  short_description: "Coordinate safe parallel repository work"
  default_prompt: "Use $multi-agent-work-coordinator to create a safe work graph with exclusive ownership, execution waves, and verified handoffs."
