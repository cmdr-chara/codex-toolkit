interface:
  display_name: "Multi-Agent Work Coordinator"
  short_description: "Coordinate safe parallel work across owned resources"
  default_prompt: "Use $multi-agent-work-coordinator to create a safe work graph with exclusive mutable-resource ownership, dependency waves, and verified handoffs."
